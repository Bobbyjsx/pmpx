export async function showAnimation() {
   // Import chalk-animation as an ESM module and handle its default export
   // const chalkAnimationModule = await import("chalk-animation");
   // Access the default export for ESM compatibility
   // const chalkAnimation = chalkAnimationModule.default || chalkAnimationModule;
   // const animation = chalkAnimation.rainbow("✨ pmpx: Package Manager Proxy ✨");
   // setTimeout(() => {
   //   animation.stop();
   //   console.log("\nBack to work now 😄");
   // }, 4000);
}
